% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedding.R
\name{embedding}
\alias{embedding}
\alias{embedding.default}
\alias{embedding.proximity_nystrom}
\title{The configuration a proximity matrix implies}
\usage{
embedding(x, k = 2L, ...)

\method{embedding}{default}(x, k = 2L, transform = c("sqrt", "linear"), arg = "x", ...)

\method{embedding}{proximity_nystrom}(x, k = 2L, transform = c("sqrt", "linear"), arg = "x", ...)
}
\arguments{
\item{x}{A \code{proximity} object, a symmetric numeric matrix, or a
\code{proximity_nystrom} object.}

\item{k}{Number of dimensions to retain.}

\item{...}{Passed to methods.}

\item{transform}{Dissimilarity to scale, passed to \code{\link[=as_dissimilarity]{as_dissimilarity()}}.}

\item{arg}{The name to report for \code{x} in error messages. Callers that hold
the object under a different name pass their own; there is no reason to
set it interactively.}
}
\value{
An \code{n} by \code{k} matrix of coordinates, centred, with the dimensions in
decreasing order of the variance they carry.
}
\description{
Reduces a proximity to a cloud of \code{n} points in \code{k} dimensions, by classical
multidimensional scaling of the induced dissimilarity. This is what
\code{\link[=protest]{protest()}} superimposes and what a two-dimensional plot of a forest shows.
}
\section{Methods (by class)}{
\itemize{
\item \code{embedding(default)}: Classical scaling of a dense proximity matrix.

\item \code{embedding(proximity_nystrom)}: The same configuration, read off the stored factor of
a Nystrom approximation without reconstructing the matrix.

Only \code{transform = "sqrt"} is available. The squared dissimilarity is then
\eqn{\mathbf{1}\mathbf{1}^{\top} - \tilde{P}}, whose centred form is
\eqn{\tfrac{1}{2} (JL)(JL)^{\top}}, and the configuration is
\eqn{JL V / \sqrt{2}} with \eqn{V} the eigenvectors of
\eqn{(JL)^{\top}(JL)}. The linear transform squares to \eqn{(1 - P)^2},
which does not factor through \eqn{L} and would need the dense matrix.

The diagonal of \eqn{\tilde{P}} is not one, so this configuration and the
one obtained by reconstructing the matrix and scaling it disagree by
exactly the diagonal error that \code{\link[=summary.proximity_nystrom]{summary.proximity_nystrom()}} reports.
They coincide when the landmarks span the sample.

}}
\section{The same thing twice}{

Classical scaling of \eqn{\sqrt{1 - P}} and the principal components of
\eqn{P} read as a kernel are the same computation. With
\eqn{D^2 = \mathbf{1}\mathbf{1}^{\top} - P} and \eqn{J} the centring
operator, \eqn{J \mathbf{1} = 0} kills the first term and
\deqn{G = -\tfrac{1}{2} J D^{2} J = \tfrac{1}{2} J P J,}
so the scaling never sees the dissimilarity at all. It holds exactly when
the diagonal of \eqn{P} is one, which is why \code{\link[=nystrom]{nystrom()}} objects, whose
diagonal is not one, are handled by their own method rather than by
reconstructing the matrix and pretending.
}

\section{What it costs}{

On a dense matrix, the eigendecomposition of the centred matrix, so
\eqn{O(n^3)}. On a \code{\link[=nystrom]{nystrom()}} object the same configuration comes out of an
\eqn{r \times r} decomposition of the stored factor, at \eqn{O(nr^2)}, and
the \eqn{n \times n} matrix is never formed.
}

\examples{
\dontshow{if (requireNamespace("randomForest", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(1)
rf <- randomForest::randomForest(Species ~ ., data = iris, ntree = 200)
px <- as_proximity(rf, newdata = iris)
head(embedding(px, k = 2))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=protest]{protest()}}, which superimposes two of these, and \code{\link[=nystrom]{nystrom()}}.
}
