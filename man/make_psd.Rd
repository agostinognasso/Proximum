% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make-psd.R
\name{make_psd}
\alias{make_psd}
\title{Repair an indefinite proximity matrix}
\usage{
make_psd(px, method = c("clip", "flip", "shift"), rescale = TRUE, tol = 1e-08)
}
\arguments{
\item{px}{A \code{proximity} object, or a symmetric numeric matrix. It must not
contain \code{NA}: an out-of-bag proximity computed from too few trees has
undefined pairs, and no eigendecomposition can be had of a matrix with
holes in it. Grow more trees.}

\item{method}{One of \code{"clip"}, \code{"flip"}, \code{"shift"}.}

\item{rescale}{Return the corrected matrix with a unit diagonal.}

\item{tol}{Eigenvalues below \code{-tol * max(abs(lambda))} count as negative.}
}
\value{
A \code{proximity} object with the same attributes as \code{px}, plus
\code{psd_correction} recording the method used and the smallest eigenvalue that
was repaired. When \code{px} was already positive semi-definite the matrix is
returned unchanged and \code{psd_correction} is \code{"none"}.
}
\description{
The out-of-bag proximity is not positive semi-definite (see
\code{\link[=summary.proximity]{summary.proximity()}}), so it is not a kernel, and every method that assumes
one (centered kernel alignment, the RV coefficient, kernel PCA, classical
multidimensional scaling with an exact embedding) is applied to it at the
user's peril. \code{make_psd()} projects it onto the cone of positive
semi-definite matrices first, and records how.
}
\section{Which correction}{

All three act on the eigenvalues \eqn{\lambda_i} of the symmetrised matrix
and leave the eigenvectors alone. They differ in what they assume the
negative eigenvalues \emph{mean}.
\itemize{
\item \code{"clip"} sets \eqn{\lambda_i \leftarrow \max(\lambda_i, 0)}. The negative
directions are noise and are discarded. This is the nearest positive
semi-definite matrix in Frobenius norm, and the default.
\item \code{"flip"} sets \eqn{\lambda_i \leftarrow |\lambda_i|}. The negative
directions carry signal whose sign is an artefact. Keeps the rank.
\item \code{"shift"} sets \eqn{\lambda_i \leftarrow \lambda_i - \lambda_{\min}}, that
is, adds \eqn{-\lambda_{\min}} to the diagonal. It preserves every
off-diagonal proximity exactly, at the price of declaring every
observation more similar to itself than the data said.
}

There is no correction that is right in general. \code{"clip"} is the safe
default because it changes the matrix least; \code{"shift"} is the one to use when
the off-diagonal proximities must not move, for instance when the corrected
matrix has to stay comparable with an uncorrected one.
}

\section{On rescaling}{

None of the three preserves a unit diagonal, and a proximity with
\eqn{P_{ii} \ne 1} is hard to read. With \code{rescale = TRUE} the result is
converted back to unit diagonal by the congruence
\eqn{\tilde{P} = D^{-1/2} P D^{-1/2}}, with \eqn{D} the diagonal of \eqn{P}.
A congruence by a positive diagonal matrix preserves positive
semi-definiteness, so the repair survives the rescaling.
}

\examples{
\donttest{
if (requireNamespace("randomForest", quietly = TRUE)) {
  set.seed(1)
  rf <- randomForest::randomForest(
    Species ~ ., data = iris, ntree = 200, keep.inbag = TRUE
  )
  px <- as_proximity(rf, newdata = iris, type = "oob")
  summary(px)$euclidean          # FALSE: not a kernel
  summary(make_psd(px))$euclidean # TRUE
}
}
}
\seealso{
\code{\link[=summary.proximity]{summary.proximity()}} for the diagnostic that tells you whether you
need this.
}
