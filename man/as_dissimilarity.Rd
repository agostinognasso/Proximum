% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transform.R
\name{as_dissimilarity}
\alias{as_dissimilarity}
\title{Transform a proximity into a dissimilarity}
\usage{
as_dissimilarity(px, transform = c("sqrt", "linear"))
}
\arguments{
\item{px}{A \code{proximity} object, or a symmetric numeric matrix.}

\item{transform}{\code{"sqrt"} for \eqn{\sqrt{1 - P}}, \code{"linear"} for
\eqn{1 - P}.}
}
\value{
A symmetric numeric matrix of dissimilarities with a zero
diagonal.
}
\description{
Both transforms are monotone decreasing in the proximity, but only the
square-root one is guaranteed to behave like a metric: \eqn{1 - P} can
violate the triangle inequality, while \eqn{\sqrt{1 - P}} is Euclidean
whenever \eqn{P} is positive semi-definite. Use \code{\link[=is_euclidean]{is_euclidean()}} to check
the result rather than assuming it.
}
